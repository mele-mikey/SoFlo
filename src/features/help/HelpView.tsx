import { BookOpen, FileText, GraduationCap, Keyboard, Sparkles } from 'lucide-react'
import type { ReactNode } from 'react'

export function HelpView() {
  return <main className="help-view content-view"><header className="view-intro"><p className="eyebrow">SOFLO GUIDE</p><h1>Everything starts here.</h1><p>A short guide to keeping classes, papers, and studying in one calm place.</p></header><section className="help-grid"><Help icon={<GraduationCap size={20} />} title="Set up your classes">Create a semester, then add each class. Spring classes archive after May 12 and Fall classes after December 12, while all past work stays available.</Help><Help icon={<FileText size={20} />} title="Write papers">Create a paper or import a PDF/Word document. The editor uses US Letter pages with Google Docs-style defaults and saves automatically.</Help><Help icon={<BookOpen size={20} />} title="Study your way">Make a flashcard set, then use Flashcards, Learn, Test, Match, or the local-AI Teach It Back game. Every response contributes to mastery.</Help><Help icon={<Sparkles size={20} />} title="Use local AI">AI can format imports, create flashcards, review writing, and check teach-back explanations. It runs with compact local models on your PC.</Help><Help icon={<Keyboard size={20} />} title="Useful shortcuts">Ctrl/Cmd + F finds in the current paper. Ctrl/Cmd + K opens Search. Standard undo, redo, cut, copy, paste, and Tab/Shift+Tab indentation work in papers.</Help></section></main>
}
function Help({ icon, title, children }: { icon: ReactNode; title: string; children: ReactNode }) { return <article className="help-card"><span>{icon}</span><div><h2>{title}</h2><p>{children}</p></div></article> }
